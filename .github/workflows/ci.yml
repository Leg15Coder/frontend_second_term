name: CI

on:
  pull_request:
    branches:
      - main
      - master

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: |
          # Use `npm install` in CI to allow npm to resolve and install platform-appropriate optional native packages
          # Use --legacy-peer-deps to avoid peer dependency resolution failures
          npm install --legacy-peer-deps

      - name: Lint
        run: npm run lint

      - name: Run tests (coverage)
        run: npm run test:coverage
        env:
          CI: true

      - name: Build (verify production build)
        run: npm run build
